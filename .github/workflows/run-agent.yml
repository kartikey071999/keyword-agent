name: Run Keyword Agent

on:
  push:
  workflow_dispatch:

jobs:
  run-agent:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Compile C++ agent
        run: g++ agent.cpp -o agent

      - name: Run agent
        run: ./agent
